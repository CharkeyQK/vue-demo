export function getIsShowAddCategory(state) {
  return state.categoryState.isShowAddCategory;
}
export const allOfferings = state => state.categoryState.isShowAddCategory;
export const qryFilters = function(state) {
  return state.categoryState.isShowAddCategory;
};
