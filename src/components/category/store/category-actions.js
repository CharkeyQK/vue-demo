import categoryApi from './category-api'
import * as types from './category-mutation-types'

const actions = {
    getAllorderState: function ({ commit,state }, params) {

    }
};
module.exports = actions;
