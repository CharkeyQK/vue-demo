import * as types from './category-mutation-types'

// initial state
const state = {
  isShowAddCategory: true
};

const mutations = {
    // 回调
    [types.RECEIVE_OFFERINGS] (state, resultData) {

    }
};

export default{state, mutations}
